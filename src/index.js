import React from "react";
import ReactDOM from "react-dom";
import IteratingEmployeeList from "./components/IteratingEmployeeList";


var user = {
    name: "M"
}

ReactDOM.render(<IteratingEmployeeList />, document.getElementById("root"))


 